{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {%
    include "@roblib_vre_d8/navigation/breadcrumb/breadcrumb.twig" with {
      modifier_class: '',
      heading: 'Breadcrumb'|t,
      breadcrumb: breadcrumb
    } only
  %}
{% endif %}
